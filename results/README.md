# Results Directory

Store generated charts, evaluation tables, confusion matrices, and submission files in this directory.

Generated outputs are excluded from version control by default. Remove the relevant patterns from `.gitignore` if you want to publish selected figures.
